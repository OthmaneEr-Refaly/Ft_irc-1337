for othman :

JOIN :

In JOIN reply, fix the prefix to :<nick>!<user>@<host> JOIN :#chan (not just :nick JOIN).
After that, always send:
331 if no topic / 332 + 333 if topic exists
353 with member list (@nick for op)
366 end of names list

MODE :

In MODE reply, fix the prefix to :<nick>!<user>@<host> MODE :#chan +itkl (not just :nick MODE #chan +itkl).

PART :
 :opuser!opuser@<host> PART #edgecase :leaving